# Инструкция по использованию страницы "История клуба"

## Что было сделано ✅

История клуба полностью переработана:
- ✅ **Удалены встроенные стили** - все `style="..."` атрибуты удалены
- ✅ **Добавлены CSS классы** - используется семантическая разметка с БЭМ нотацией
- ✅ **Подключен внешний CSS** - файл `page-history.css` линкуется в начало HTML
- ✅ **Полная рефакторизация** - все 4 основные секции переведены на классы

## Файлы проекта

```
wp-content/themes/arsenal/
├── page-history.php                    # WordPress шаблон страницы (опционально)
├── template-parts/
│   └── history-content.php             # PHP компонент с i18n (опционально)
└── assets/
    ├── css/
    │   └── page-history.css            # ✨ Главный CSS файл (670+ строк)
    └── js/
        └── page-history.js             # JavaScript для интерактивности (опционально)

HISTORY-PAGE-HTML-CONTENT.txt           # 👈 Чистый HTML (используй этот файл!)
```

## Как использовать в WordPress

### Вариант 1: Скопировать HTML в страницу (Рекомендуется) ⭐

1. **Откройте WordPress админку** → Pages (Страницы)

2. **Создайте или отредактируйте страницу** "История клуба"

3. **Нажмите "+" и выберите "Custom HTML"** (Пользовательский HTML блок):
   ```
   Все блоки → Форматирование → Пользовательский HTML
   ```

4. **Откройте файл** `HISTORY-PAGE-HTML-CONTENT.txt`

5. **Скопируйте весь контент** (начиная с `<link rel="stylesheet"...`)

6. **Вставьте в блок Custom HTML** в WordPress

7. **Сохраните и опубликуйте** страницу

8. **Проверьте результат:**
   - Откройте страницу на фронтенде
   - CSS должен загрузиться и отобразить правильные стили
   - Проверьте все 4 секции: "История", "Временная шкала", "Рекорды", "Стадионы"

### Вариант 2: Использовать WordPress шаблон (Альтернативный)

Если у вас уже есть файлы:
- `wp-content/themes/arsenal/page-history.php`
- `wp-content/themes/arsenal/template-parts/history-content.php`

...то можете использовать их вместо HTML, но HTML вариант проще и не требует доступа к файлам темы.

## Структура HTML после рефакторизации

```html
<link rel="stylesheet" href="/wp-content/themes/arsenal/assets/css/page-history.css">

<div class="club-history-container">
  
  <!-- Секция 1: История -->
  <section class="history-intro">
    <h2 class="history-intro__heading">История клуба</h2>
    <p class="history-intro__text">...</p>
  </section>

  <!-- Секция 2: Временная шкала -->
  <section class="timeline-section">
    <div class="timeline-years">
      <div class="timeline-year">
        <div class="timeline__year-badge">2018</div>
        <div class="timeline__dot timeline__dot--champion"></div>
      </div>
      <!-- ... остальные годы ... -->
    </div>
  </section>

  <!-- Секция 3: Рекорды и достижения -->
  <section class="achievements-section">
    <div class="achievements-grid">
      <div class="achievement-card">...</div>
      <div class="stat-card stat-card--full-width">...</div>
    </div>
  </section>

  <!-- Секция 4: Стадионы -->
  <section class="stadiums-section">
    <div class="stadiums-grid">
      <div class="stadium-item">...</div>
    </div>
  </section>

</div>
```

## CSS Классы (БЭМ нотация)

### История (`.history-intro`)
- `.history-intro__heading` - заголовок
- `.history-intro__text` - текст описания

### Временная шкала (`.timeline-section`)
- `.timeline-years` - flex контейнер для годов
- `.timeline-year` - отдельный год
- `.timeline__year-badge` - надпись года (2018, 2019...)
- `.timeline__dot--champion` - красный кружок (чемпион)
- `.timeline__dot--gold` - золотой кружок (медаль)
- `.timeline__dot--red` - светлокрасный кружок
- `.timeline__dot--accent` - акцентный кружок

### Достижения (`.achievements-grid`)
- `.achievement-card--primary` - карточка с градиентом
- `.achievement-card--white` - белая карточка с бордюром
- `.stat-card` - статистическая карточка
- `.stat-card--full-width` - карточка на всю ширину (последняя)
- `.stat-card__label` - описание статистики
- `.stat-card__value` - значение статистики
- `.stat-card__bold` - жирный текст внутри

### Стадионы (`.stadiums-section`)
- `.stadiums-grid` - сетка стадионов (2 колонны)
- `.stadium-item` - элемент стадиона
- `.stadium-item__icon` - иконка стадиона (🏟️)
- `.stadium-item__info` - информация
- `.stadium-item__name` - название стадиона
- `.stadium-item__location` - локация и вместимость

## CSS Переменные (Design Tokens)

Все цвета и размеры определены в `:root` CSS файла:

```css
:root {
    --color-primary: #900;              /* Основной красный (Арсенал) */
    --color-primary-light: #ff1a1a;     /* Светлокрасный */
    --color-primary-accent: #f33;       /* Акцентный красный */
    --color-gold: #f0b100;              /* Золото */
    --color-text-dark: #0a0a0a;         /* Тёмный текст */
    --color-text-secondary: #4a5565;    /* Вторичный текст */
    --color-bg-light: #f9fafb;          /* Светлый фон */
    --font-primary: 'Inter', sans-serif;
    --radius-md: 10px;                  /* Скругление углов */
    --shadow-md: 0px 10px 15px rgba...  /* Тень */
}
```

## Адаптивность (Responsive)

CSS файл включает медиа-запросы для всех устройств:

- **1440px** - основная версия (desktop)
- **768px** - планшеты (tablet)
- **480px** - мобильные (mobile)

Проверьте на разных размерах экранов:
```
Desktop:  1440px → полная ширина
Tablet:   768px → 1 колонна вместо сетки
Mobile:   480px → горизонтальный скролл для временной шкалы
```

## Запуск локально на Laragon

```powershell
# Окно PowerShell в папке c:\laragon\www\arsenal\

# 1. Проверьте что CSS файл на месте:
Test-Path -Path "wp-content\themes\arsenal\assets\css\page-history.css"
# Должно вывести: True

# 2. Откройте браузер:
# http://arsenal.test

# 3. Создайте страницу "История" и добавьте HTML из HISTORY-PAGE-HTML-CONTENT.txt
```

## Важные замечания ⚠️

### Путь к CSS файлу
```html
<!-- В файле HISTORY-PAGE-HTML-CONTENT.txt стоит: -->
<link rel="stylesheet" href="/wp-content/themes/arsenal/assets/css/page-history.css">
```

Если сайт установлен в подпапку (например `/football/`), нужно изменить на:
```html
<link rel="stylesheet" href="/football/wp-content/themes/arsenal/assets/css/page-history.css">
```

### Для продакшена
На продакшене (1779917-cq85026.twc1.net) путь остается прежним - WordPress обычно устанавливается в корень домена.

### Если CSS не загружается
1. **Проверьте в DevTools** (F12 → Network tab) - загружается ли файл?
2. **Проверьте путь** - правильно ли указан путь к файлу?
3. **Очистите кэш браузера** - Ctrl+Shift+Delete
4. **Проверьте файл существует:**
   ```powershell
   Test-Path "c:\laragon\www\arsenal\wp-content\themes\arsenal\assets\css\page-history.css"
   ```

## Отредактировать контент

Контент страницы находится в файле `HISTORY-PAGE-HTML-CONTENT.txt`:

```html
<!-- История -->
<p class="history-intro__text">
  ФК Арсенал (Дзержинск) — молодой и амбициозный клуб, основанный в 2018 году...
</p>

<!-- Временная шкала: годы и события -->
<div class="timeline__year-badge">2018</div>
<div class="timeline__dot timeline__dot--champion"></div>

<!-- Достижения: карточки -->
<div class="achievement-card achievement-card--primary">
  <h4 class="achievement-card__title">Чемпион Первой лиги</h4>
  ...
</div>

<!-- Статистика -->
<div class="stat-card">
  <div class="stat-card__label">Наибольшее количество матчей</div>
  <div class="stat-card__value">Александр Скшинецкий – 65</div>
</div>

<!-- Стадионы -->
<h4 class="stadium-item__name">РЦОП-Стайки</h4>
<p class="stadium-item__location">Минск • Вместимость: 1,500</p>
```

## Итоги ✨

- **Файл для использования:** `HISTORY-PAGE-HTML-CONTENT.txt`
- **CSS подключен:** `/wp-content/themes/arsenal/assets/css/page-history.css`
- **Все стили удалены:** только классы, семантическая разметка
- **Готов к WordPress:** просто скопируй в Custom HTML блок
- **Адаптивный:** работает на всех размерах экранов
- **Поддерживаемый:** легко редактировать контент и стили

---

**Версия:** 2.0 (с внешним CSS)  
**Дата:** Январь 2026  
**Проект:** ФК Арсенал Дзержинск (Arsenal WordPress Theme)
